---
##################################################################################
# Datacenter where to remove snapshots
##################################################################################
datacenter:

  primary:

    # Available datacenters: STG-HX-DC, RTP-Staging, PAE-HX-DC, Plat-Ops, Dev Engineering, cdra-Build-Development, PAE-Customer-Build, RTP5, Alln1 (required) (string)
    name: 'PAE-HX-DC-2'

    # Snapshots maximum age (required) (integer)
    max_age: 7
